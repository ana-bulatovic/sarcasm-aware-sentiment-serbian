"""CLI skripte projekta (ulazne tačke ka `src/`).

Mapa podfoldera:
  collection/     — skupljanje komentara (YouTube, Twitter, Reddit, …)
  dataset/        — build šablona, draft anotacija, statistike
  modeling/       — BERTić fine-tune, evaluate, predict, splitovi
  baselines/      — TF-IDF + klasični ML (NB / LR / SVM)
  preprocessing/  — čišćenje: pipeline (raw→interim) i baseline tekst
"""
