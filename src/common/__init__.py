"""Zajedničke utilite koje koriste svi moduli.

Fajlovi:
  - ``config.py`` — YAML/.env i putanje projekta
  - ``schema.py`` — kolone dataseta i dozvoljene labele
  - ``io_utils.py`` — JSONL/CSV čitanje-pisanje
  - ``source_utils.py`` — URL/alias → platforma
  - ``language.py`` — heuristički filter srpskog
  - ``training_flags.py`` — class weights (baseline + BERTić)
  - ``stdio_utf8.py`` — UTF-8 na Windows konzoli
"""
